/**
 * io-ts-docopt
 * Decode docopt output with io-ts
 */
import * as t from 'io-ts';
import * as E from 'fp-ts/Either';
import { DocoptOption } from 'docopt';
export { withEncode } from 'io-ts-types/lib/withEncode';
/**
 * Decode the output of `docopt` with an io-ts codec.
 */
export declare function decodeDocopt<C extends t.Any>(codec: C, docstring: string, { argv, help, version, options_first, exit }?: DocoptOption): E.Either<t.Errors, C['_O']>;
//# sourceMappingURL=io-ts-docopt.d.ts.map